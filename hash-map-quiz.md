## ArrayList

1. How is an ArrayList easier to use than an Array. What is it's killer feature.
2. What method do you call to add items to an ArrayList and what do you pass that method as an argument?
3. What method do you call to update items in an ArrayList and what arguments do you pass that method?
4. What method do you call to delete items in an ArrayList and what arguments do you pass that method?
5. Define a variable that holds an ArrayList of ints?
6. Can an ArrayList hold an object?

## Agile

1. Describe Waterfall style software development vs Agile software development.
2. What are some of the values of the Agile Philosophy and what do they mean to you.
3. Talk about some agile principles and what they mean to your group.
4. What are the three roles on a scrum team
5. What is a Sprint in the Scrum or other Agile software methodologies
6. What are the three common questions that every team member answers at the daily stand up?
7. When do retrospectives take place.

## HashMap

1. An Array or ArrayList has an index and a value. A Map like HashMap has a ___ and a value?
2. You add an item to an Array by assigning to a specific index `array[index] = value`.
   You add an item to an ArrayList by calling arrayList.add(value) and the index is assigned based on position.
   You add an item to a HashMap by calling the hashMap.put(key, value) method
3. Do HashMaps store things in the order they were added.
4. You get an item to an Array by assigning to a specific index `String value = array[index]`.
   You get an item to an ArrayList by calling arrayList.get(value) and the index is assigned based on position.
   You get an item to a HashMap by calling the hashMap.get(key) method
5. How do you iterate through a HashMap (.values or .keySet). Why is it unusual to iterate through a HashMap?

 How do you iterate through a HashMap. Why is it unusual to iterate through a HashMap?

You get an item to an Array by assigning to a specific index `String value = ____`.
You get an item to an ArrayList by calling arrayList.___(____) and the index is assigned based on position.
You get an item to a HashMap by calling the hashMap.____(____) method



You add an item to an Array by assigning to a specific index `________ = value`.
You add an item to an ArrayList by calling ``arrayList.___(___)`` and the index is assigned based on position.
You add an item to a HashMap by calling the `hashMap.put(______, ______)` method
